/**
 * EnvironmentalContext store.
 * Controls safety filter behavior (Constitution Principle III).
 *
 * Task: T031
 */

export interface EnvironmentalContext {
  fireRated: boolean;
  marineGrade: boolean;
  highVibration: boolean;
}
